extend type Query {
 # get the google authentication url that frontend will use to redirect user
 getGoogleAuthUrl: String!
 #validate the token
 validateToken: Boolean!
}

extend type Mutation {
 # logout the current user and clear authentication cookies
 logout: Boolean!
}